export BROWSER=zen-browser
export EDITOR=~/.local/bin/code

export ELECTRON_OZONE_PLATFORM_HINT=wayland

alias ls="eza -lg"
alias lla="eza -lag"
alias ll="eza -lg"

eval "$(oh-my-posh init bash --config ~/.config/ohmyposh/theme.json)"